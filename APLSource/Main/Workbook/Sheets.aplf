xml←Sheets;⎕IO;sheet;body
⍝⍝⍝: This function builds the <sheets> tags for xl/workbook.xml
⎕IO←0
sheet←{
	name sheetId relId←⍵
	t←'sheet'
	n←⊂'name' name
	s←⊂'sheetId' (⍕sheetId)
	r←⊂'r:id' ('rId',⍕relId)
	t(⊃,/n s r) tag ''
}

⍝ TODO(nathan): duplicate expression
body←(sheet⍤1⊢{⍵{⍵,(1+⍳≢⍵),⍪⍺.rId}∪1⌷⍤1⊢⍵.Table}#.Worksheets)
xml←'sheets'tag body
