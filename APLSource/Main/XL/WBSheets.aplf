 WBSheets←{
     wb←⍵
    ⍝. This function builds the <sheets> tags for xl/workbook.xml
     sheet←{
         name sheetId relId←⍵
         t←'sheet'
         n←⊂'name'name
         s←⊂'sheetId'(⍕sheetId)
         r←⊂'r:id'('rId',⍕relId)
         t(⊃,/n s r)tag''
     }

     body←sheet⍤1⊢{⍵,(1+⍳≢⍵),⍪wb.WSRID}WSNames wb

     'sheets'tag body
 }
