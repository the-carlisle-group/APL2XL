CFAdd←{
    cfs←⍵
    wb←⍺
    names←'NumberFormat'  'Fill' 'Border' 'WrapText' 'Horizontal' 'Vertical' 'ShrinkToFit' 'Indent' 'Font'
    defaults←9⍴⊂⍬
    expand←{(⍵,defaults[⍸~⍺])[⍋⍒⍺]}
    vals←{⍵{⍵ expand ⍺⍎¨names/⍨⍵}0≠⍵.⎕NC names}¨cfs
    wb.CFStyles,←vals
    wb
}