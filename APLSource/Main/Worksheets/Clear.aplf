 t←{column}Clear v
 :If (0<⎕NC)'Table'
     c←{⍵:1 ⋄ 80=⎕DR column:_colselect column ⋄ column}(0≥⎕NC)'column'
     bmp←~c{0=≢⍵:(≢Table)⍴1 ⋄ 80=⎕DR ⍵:(⊂lc ⍵)≡¨lc¨⍺⌷[1]Table ⋄ (⊂⍵)≡¨⍺⌷[1]Table}v
     Table∘←bmp⌿Table
     t←Select''
 :Else
     t←ColumnNames
 :EndIf

 ⍝ TODO: Fix bug where id's arguments are a different type
 ⍝ This is completely confusing