 Chess←{
     Setup←{
      bl←'♟♜♞♝♛♚'
      wh←'♙♖♘♗♕♔'
      back←{⊂{⍵,2↓⌽⍵}1↓⍵}
      pawns←{8⍴⊃⍵}¨bl wh
      board←↑({⍵,⌽⍵}1 1 0 0)\(back bl),pawns,(back wh)
     }

    Export←{
     range←⎕NS''
     range.Value←⍵
     range.Address←'A1'
     range.Fill←1↓⍤1⊢8 9⍴('solid' 'd4b790' 1) ('solid' '87673b' 1)
     (sheet←⎕NS'').(Name Ranges)←'board'range
     (wb←⎕NS'').(FileName Sheets)←'c:\r\chess'sheet
     ##.Main.Export wb
    }
    Export Setup ''
 }
